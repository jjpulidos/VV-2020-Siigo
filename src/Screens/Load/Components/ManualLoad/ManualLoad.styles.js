import styled from 'styled-components';

export const ManualLoadContainer = styled.div``;

export const Form = styled.siv``;

export const Field = styled.div``;

export const FieldTitle = styled.p`
    margin: 0;
    font-weight
`;
